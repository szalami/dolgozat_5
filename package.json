{
  "name": "fejlesztoi_sablon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx lite-server",
    "json": "json-server --watch database.json --port 8000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.17.1",
    "lite-server": "^2.6.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
